import PropTypes from 'prop-types';

const MissionBadge = ({ joined }) => (
  <div>MissionBadge</div>
);

export default MissionBadge;

MissionBadge.propTypes = {
  joined: PropTypes.bool.isRequired,
};
