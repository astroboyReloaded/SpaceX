import missions from '../../styles/myProfile.module.css';

const MyMissions = () => (
  <section className={missions.sectionContainer}>
    <h1>MyMissions</h1>
  </section>
);

export default MyMissions;
